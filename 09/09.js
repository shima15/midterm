function countDigits(number){
    if(number <= 0){
       return -1;
    }else if(number >= 0){
        var result = number.length;
        return result;
    }else if(number //小数点をどう表すのかわからない)
    )
    return //少数点以下
}